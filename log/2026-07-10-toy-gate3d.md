# 2026-07-10 — gate (iii): next-lever program (post-attempt-4 review approval)

Reconvene approved the gap inventory; levers proceed in the approved order,
pre-registered here, each a SINGLE-variable delta against the (b1) baseline
(--attn --aux, no shortk, 200k). Levers 1a and 2 target DIFFERENT gaps
(d=2 drop vs sharpness), so they run in parallel per the standing
parallel-jobs allowance; attribution stays clean.

**RESULTS-toy flag (per review):** funnels failing CONDITIONALLY even at
K=512 while the bespoke head passes with identical whitening is a real
finding — conditional-model-specific, distinct from the K=128 refusal
(which is correct designed behavior). Carry both, separately, into
RESULTS-toy.

## Lever 1a (d=2 fix, variable 1): one-hot d-embedding
Tokens gain 4 always-allocated one-hot slots for d in {2,4,8,16} (zero when
disabled — shapes stable). Hypothesis: the scalar d/DMAX=0.125 at d=2 is too
weak for the head to sharply separate real dims from padding; a categorical
signal lets the encoder/head specialize. Config: b1 + --donehot, 200k, p1
criteria. READOUT: gmm-d2 mode_recovery vs 0.67, dwell-d2 vs 0.50, d=2 logZ
vs ln(covered mass). Attribution caveat (logged): token width 37->41 changes
the init RNG draw even with slots zeroed, so lever-2's run shares this
caveat vs b1; loss curves reported for both.
## Lever 2 (sharpness): 400k steps on the b1 recipe
Config: b1 exactly, --steps 400000 (cosine stretched accordingly), p1
criteria. READOUT: does SW2 close toward the 2x-bespoke bar as the loss
plateau (~1.38) resolves with longer cosine? If SW2 stalls with loss flat,
length is exonerated and lever 3 (head capacity) fires next.

## Submissions
- lever 1a: job 15627064, config 3e095d407d20 -> results/gate3_d1.json
- lever 2:  job 15627065, config 15bbf2047dd2 -> results/gate3_400k.json

## Lever 1a RESULT (job 15627064) — REFUTED for the d=2 drop
mode_recovery identical (gmm-d2 0.67, dwell-d2 0.50), gmm-d2 logZ -0.72 ~
ln(covered mass); other movements within seed noise; n_pass 1/10 unchanged.
The one-hot d-signal is not the mechanism.

## d=2 coverage diagnostic (pre-registration; runs BEFORE lever 1b)
The drop now survives shortK-ablation, width, and tokenization. Competing
hypothesis: the review's "below a provable ceiling" transferred M2's
2-PARAMETER gmm result; the zoo gmm has free means/covs/weights, where modes
unvisited by all 4 chains at d=2 may be genuinely unidentifiable — an
oracle-ceiling issue mirroring finding 3, not a model bug. TEST (eval-only,
b1 checkpoint): d=2 rows evaluated with K=512 contexts (better coverage).
READOUT: if gmm-d2/dwell-d2 mode_recovery -> ~1.0 and logZ -> 0 with K=512,
the d=2 "bug" RECLASSIFIES as context-coverage-limited (criteria/eval-design
question for the reconvene); if unchanged, it is a real model failure and
lever 1b (family balance) fires as pre-registered.
- submitted: job 15632325, config 01f32dea6d64

## Lever 2 RESULT (job 15627065) — training length EXONERATED
400k vs 200k: n_pass 1/10 unchanged; per-row movements at noise level in
both directions. NOTE the dwell-d4 swing (ESS 20.3% -> 0.3% between runs
differing only in schedule length + init caveat): run-to-run variance at
these ESS levels is substantial — future lever comparisons must not
over-read single-row deltas; the composite count and multi-row patterns are
the readouts. Per pre-registration, lever 3 (head capacity) fires.

## d2-coverage diagnostic crash + resubmission
Job 15632325 failed: ScopeParamShapeError 41-vs-37 — the always-allocated
one-hot slots broke BACKWARD compat with pre-edit checkpoints (my "shapes
stable" claim held only for new runs). Fix: --legacy-tokens strips the 4
slots at eval; resubmitted on the b1 checkpoint.

## Lever 3 (sharpness): head capacity (pre-registration)
Config: b1 recipe + head_hidden (256,256,256) -> (512,512,512), single
variable, 200k, p1 criteria. READOUT: composite count + SW2 rows vs the
2x-bespoke bars; expectation (50%) that sharpness is head-limited given the
encoder was exonerated by diag (a) and length by lever 2.
- resubmitted d2cov: job 15633129 (31d088fae5bb); lever 3: job 15633130 (ece944ec0191)

## d2-coverage diagnostic RESULT (job 15633129) — INCONCLUSIVE as run
gmm-d2 mode_recovery 0.67 and logZ -0.770 UNCHANGED at K=512 — but the
premise failed: the K=512 context still gives modes 0/5 only 1 point each
(verified: visit counts [1,201,94,154,61,1]). MALA never hops between
well-separated modes at d=2, so K alone cannot produce covering contexts.
The reclassification question (model bug vs oracle ceiling) stays open; the
right instrument is TEMPERATURE (stage-0 protocol included T in {1,2,5};
tempered chains do hop). Whether tempered EVAL contexts are periphery or an
eval-design change is a reconvene question — the diagnostic below produces
the evidence either way.

## Lever 3 RESULT (job 15633130) — head capacity EXONERATED
1/10 unchanged; SW2 movements mixed at noise level. Sharpness is now not:
pathway (diag a), length (lever 2), encoder width (probe), head width
(lever 3).

## Next pair (pre-registration)
- T=5 d=2 eval diagnostic (eval-only, b1 ckpt): d=2 contexts at T=5, K=128.
  READOUT: d=2 mode visit counts become covering; if mode_recovery -> ~1 and
  logZ -> 0, the d=2 drop is CONTEXT-COVERAGE-limited and the fix (tempered
  contexts) goes to the reconvene as an eval/protocol decision. If coverage
  is achieved and the model STILL drops modes, it is a real model bug and
  lever 1b fires.
- Lever 4 (sharpness): pad-weight 0.1 — the one systematic difference
  between conditional (DMAX-padded, 75% noise dims at d=4) and bespoke
  (true-d) setups never yet tested. Loss down-weights padding dims x0.1
  (not masked to 0: padding must stay calibrated for the certificate's
  DMAX-space density). Single variable vs b1, 200k, p1 criteria.
- submitted: T5-diag job 15635457 (4f321104776b); lever-4 job 15635459 (1713f8f23906)
