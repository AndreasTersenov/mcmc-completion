# 2026-07-09 (evening) — Karpathy gate (i): single-target FM overfit, no context

## Hypothesis
The FM head (reused jax_flows core) overfits one zoo target at GPU scale, and
the full certificate loop (heun samples -> CNF log-density with exact
divergence -> SNIS vs true energy -> N-doubling) closes with a non-vacuous,
stable certificate. The CPU-scale executable twin
(tests/test_fm_overfit_gate.py) is green and committed.

## Setup
scripts/gate1_overfit.py: d=4 GMM (seed 1042), 100k exact train samples,
TimeConditionedMLP (256,256,256), 20k steps, batch 1024, cosine 2e-3.
Eval in f64: 16384 samples, CNF logq (200 heun steps, exact divergence),
snis_certificate (stage-0 estimators), sliced-W2^2 vs exact with same-p floor.

## Pre-registered PASS criteria
ESS/N(2N) >= 5%, doubling-stable, |logZ-hat| <= 0.1, SW2^2 <= 3x same-p floor.
(A well-trained flow on a smooth 4-d GMM should sit far inside these: stage-0
frontier says ESS 5% ~ D2 = 3.0, and per-target flows typically reach D2 < 1.)

## Expectation
PASS with ESS/N in the 20-90% range; the riskiest piece is the first
real-model use of cnf_logpdf (validated so far against the analytic Gaussian
field only) — a bug there shows up as a systematically biased logZ-hat with
healthy-looking SW2.

## Job (pre-submission record)
- script: scripts/slurm/gate1.sh -> scripts/gate1_overfit.py
- resources: MIG h100_20gb, 40 min cap, 24G
- expected outcome: prints GATE1-PASS + results/gate1.json with the numbers
- config hash: 5cc88c2b4009
- submitted: job ID 15583499

## Result — GATE1-PASS (job 15583499, 33 s elapsed on a MIG slice)
loss 11.86 -> 3.36 (20k steps, 28 s); ESS/N(N)=98.09%, ESS/N(2N)=98.06%,
stable; logZ-hat = -0.0006 (true 0); D2-hat = 0.0196; SW2^2 = 0.0019 vs
same-p floor 0.0023 (at floor). All pre-registered criteria met with huge
margin.

## Updated belief
The bespoke reference quality (baseline-2) at d=4 is ESS ~ 98% — that is the
bar P1's "within 2x of baseline-2" clause references. cnf_logpdf is confirmed
on a real trained model (logZ within 6e-4), so certificate numbers downstream
are trustworthy. MIG slices are sufficient for gate-scale jobs (33 s!) —
full training will need full H100s but gates stay on the cheap pool.
