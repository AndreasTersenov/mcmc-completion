# 2026-07-10 — gate (iii) attempt 4 program (post-review directives)

Executing log/2026-07-10-reconvene-gate3-attempt3.md in order; frozen core
untouched; criteria amendments are session periphery, justified by the
review.

## (a) Pathway diagnostic (pre-registration)
gate-2 pair-overfit rerun with the NEW architecture (--attn 2 --aux, no
shortk; identical seeds/settings to the green deep-sets gate-2 otherwise).
READOUT: if the attention pathway cannot overfit two pairs to gate-2 levels
(ESS >= 5%, swap control >= 3x), the sharpness gap is a pathway/objective
bug and width is NOT the lever. Expected: PASS at deep-sets levels or above
(attention should not hurt a 2-pair overfit).

## (b1) d=2 mode-drop, variable 1: shortK augmentation (pre-registration)
Full attempt-3 config MINUS --shortk (single toggle), out=gate3_noshortk.json.
READOUT (d=2 rows only; PASS/FAIL line void — old criteria): does gmm-d2
mode_recovery rise above 0.67 / logZ move toward 0 / dwell-d2 above 0.50?
Hypothesis (review): shortK creates genuine low-d ambiguity (a K=8 context on
a d=2 multimodal target is compatible with many mode sets), teaching the
model to hedge at d=2 even for K=128 contexts. If confirmed, the fix is
K-aware augmentation (restrict shortK to d >= 4) rather than dropping it.

## Parallel capacity probe (already in flight, relabeled)
Job 15617204 (--wide): informs finding-2's capacity branch; verdict line
void; per-row ESS on gmm/dwell mid-d is the readout.

## (c) Re-specified gate-(iii) criteria (pre-registered BEFORE the next gate run)
Mirroring the frozen P1 composite this gate guards (review directive):
- Per-target composite: ESS/N(2N) >= 1% (D2_hat <= 4.6) AND doubling-stable
  AND SW2^2 <= max(2x bespoke_ref, 0.1), where bespoke_ref = same-target
  UNCONDITIONAL FM (whitening-study recipe: (256,256), 4k steps, context-std
  whitening) trained in-job — the gate-scale proxy for frozen baseline-2.
- Funnel rows (finding 3: sigma_v unidentifiable at K=128, oracle prior-wide
  until K=512): evaluated with K=512 contexts for the composite. Their K=128
  behavior is reported separately as a CERTIFICATE row: honest refusal =
  (ESS < 1% OR unstable) = certificate success, not model failure. Do not
  chase funnel ESS at K=128.
- mode_recovery: mandatory column, all rows.
- logZ-hat: reported, NOT gating (P1 has no logZ clause; mode drop gates via
  SW2 vs bespoke_ref and is visible in mode_recovery).
- GATE3-PASS: >= 8/10 targets pass their composite.

## Submissions
- (a) job 15619456, config a8537c6ea401 -> results/gate2_newarch.json
- (b1) job 15619457, config 61914a9c4827 -> results/gate3_noshortk.json

## Implementation note
gate3_minizoo.py extended AFTER (b1)'s submission but strictly additively
(--criteria defaults to legacy; the queued b1 command line is unaffected even
if it reads the newer file at start; its logged config hash refers to the
submission-time file). The p1 branch: per-target in-job bespoke reference
(unconditional FM, whitening-study recipe), funnel composite at K=512 +
K=128 refusal row, mode_recovery everywhere, logZ reported not gating.
