# 2026-07-10 — Karpathy gate (iii), attempt 3 (encoder arm)

## Pre-registration
Single substantive change vs attempt 2 (whitening studied and kept as-is,
see reconvene log): the conditional pathway is upgraded in one shot as the
approved "mode-completion + hybrid scale" fix —
  --attn   2 self-attention token-mixing blocks before pooling (the encoder
           can relate energy readings across the landscape),
  --aux    4 per-chain summary tokens (whitened mean/std, energy range;
           flag-marked) so scale corrections can be LEARNED,
  --shortk train-time context-length augmentation (effective K in {8,32,128}
           by chain-step masking; aux rows kept; caveat: whitening stats stay
           full-context — logged, acceptable for the coverage pattern).
Everything else identical to attempt 2: same 10 targets, same 200k steps,
batch 512, cosine 1e-3, same eval contexts (keys 9000+j), same criteria
(ESS>=5%, stable, |logZ|<=0.1, SW2^2 <= max(3x floor, 0.1), >=8/10), plus the
now-mandatory mode_recovery column.

## Expectation
Mode-drop rows (gmm/dwell d=2) heal if token mixing + short-K augmentation
give the encoder what it needs to bind context->target identity (65%);
funnel rows heal if the conditional-pathway capacity was the binding issue
(55% — less certain, mechanism only located, not proven). If EITHER group
stays at attempt-2 levels, the next lever is architecture scale (wider
encoder / more blocks), not more steps.
- config hash: 934679039d6a
- submitted: job ID 15613098

## Resubmission (scheduling only, no scientific change)
Job 15613098 cancelled at step 100k/200k: the attention encoder is ~6x
slower per step than deep-sets and the MIG slice would have hit the 1:30
wall before eval (gate script has no mid-run checkpoint). Resubmitted on a
full H100 (--gpus-per-node=1, 2:59 cap, stays on b1). Lesson for gate (iv):
attention-arm time estimates must use the measured 3.1 s/1k-step MIG pace
(~0.6-1 s/1k on full H100), and train_full.py's checkpointing is mandatory.
- resubmitted: job ID 15615537, slurm-config hash 64d0a65560f5

## Result, attempt 3 — GATE3-FAIL (job 15615537, 1/10, 32 min full H100)
warp-d2 PASSES (first ever). The mandatory mode_recovery column earns its
keep immediately: **coverage largely solved, sharpness regressed.**
- mode_recovery: gmm-d4 1.00, gmm-d8 1.00, dwell-d4 1.00 (previously
  mode-dropping); gmm-d2 0.67, dwell-d2 0.50 (partial; logZ improved
  -2.47 -> -1.14 on dwell-d2).
- But mid-d ESS fell (gmm-d4 5.3% -> 0.2%, dwell-d4 13.9% -> 0.2%): the
  encoder arm spreads mass over all modes BLURRILY — honest low ESS instead
  of confident mode-dropping. A trade in the right direction (the certificate
  now sees the problem instead of being evaded) but not a pass.
- Funnels unchanged (ESS ~0.1%): conditional-pathway capacity still the
  suspect there.

## Attempt 4 (pre-registered): single lever = ENCODER SCALE
--wide: enc_dim 128->256, enc_hidden 256->512, n_attn 2->4. Everything else
frozen at attempt-3 settings (200k steps, same eval contexts, same criteria).
Rationale: attempt 3 changed the encoder's JOB (mix tokens, learn scale);
attempt 4 gives it the capacity to do that job sharply. Expectation (55%):
mid-d rows regain ESS >= 5% while keeping mode_recovery ~1.0; funnels are
the residual risk. If the coverage-good/sharpness-bad signature persists at
this scale, next levers in order: training length (loss was plateau-noisy at
1.38-1.43), then head capacity — pre-register whichever is chosen.
- attempt 4 submitted: job ID 15617204, config hash 20d0d783a05b
